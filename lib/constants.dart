import 'package:flutter/material.dart';

class AppConstants {
  // App Name
  static const String appName = 'Bookify';

  // Colors - Eye-Care HSL Tailored
  static const Color primaryColor = Color(0xFF4A6B5D); // Muted Sage Green
  static const Color accentColor = Color(0xFF8EAB9D);  // Light Sage
  
  // Light Theme Colors
  static const Color lightBg = Color(0xFFF7F4EB);      // Warm Cream Paper
  static const Color lightCardBg = Color(0xFFFFFFFF);  // Pure White
  static const Color lightText = Color(0xFF2C302E);    // Charcoal Gray
  static const Color lightSubText = Color(0xFF6E7571); // Muted Gray
  
  // Dark Theme Colors
  static const Color darkBg = Color(0xFF121513);       // Forest Dark
  static const Color darkCardBg = Color(0xFF1C201E);   // Darker Sage Gray
  static const Color darkText = Color(0xFFE3E8E5);     // Off-White
  static const Color darkSubText = Color(0xFF9EAA9F);  // Light Gray Green

  // Default Spacing
  static const double paddingSmall = 8.0;
  static const double paddingMedium = 16.0;
  static const double paddingLarge = 24.0;
  static const double radiusMedium = 16.0;
  static const double radiusLarge = 24.0;
}
